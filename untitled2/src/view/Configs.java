package view;

import domain.Experience;
import domain.Fresher;
import domain.Intern;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class Configs {
    public static Scanner scanner=new Scanner(System.in);
    public static List<Intern> internList = new ArrayList<>();
    public static List<Fresher> fresherList = new ArrayList<>();
    public static List<Experience> experienceList = new ArrayList<>();
}
